#!/bin/bash

check_File() {
	date1=$(stat -c %y "$1")
	while true; do
		date2=$(stat -c %y "$1")
		if [ "$date2" != "$date1" ]; then
			return
		fi
	done
}

while true; do
	$1 "$2"
	echo '----------------'
	check_File "$2"
done
